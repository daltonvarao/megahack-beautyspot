<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BeautySpot</title>
  {{ style('style') }}
  {{ style('dashboard') }}
  {{ style('profile') }}
  {{ style('spot') }}
</head>
<body>
  <header class="navigation">
    <div class="logo">
      <a href="{{ route('users.dashboard.index') }}">
        <h2>BeautySpot</h2>
      </a>
    </div>
    <form action="{{route('sessions.logout')}}" method="post" class="signout">
      {{ csrfField() }}
      <button class="link" type="submit">
        Sair
        <i data-feather="log-out"></i>
      </button>
    </form>
  </header>
  @!section('content')
  <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>
  <script>
    feather.replace()
  </script>
</body>
</html>
